[build]
watchPatterns = ["/apps/corti-pipeline/**"]
buildCommand = "npm ci && npm run build:pipeline"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
